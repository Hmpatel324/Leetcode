Set
----

# new set
cache = set()

# add 
adds item to a Set
cache.add(1)

# remove
removes item from set
will throw key error if not in set
cache.remove(1)

# intersection
s1 & s2

# union 
s1 or s2
s1|s2
